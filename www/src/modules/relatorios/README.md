# `src/modules/relatorios/` — Relatórios (modularização parcial)

Namespace: `window.LiderCRM.modules.relatorios.runtime`. Conectado em
`index.html` e `app.html`. Ver `docs/modules.md` (Geração 2).

| Arquivo | Papel |
|---|---|
| `runtime/feed-runtime.js` | Runtime do feed de atividades compartilhado |

Complementa `_worker_src/worker/controllers/feed-controller.js`
(backend) e `js/relatorios.js` (tela, inclui `logFeedEvent`).
