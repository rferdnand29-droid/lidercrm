# `src/modules/configuracoes/` — Configurações (modularização parcial)

Namespace: `window.LiderCRM.modules.configuracoes.runtime`. Conectado
em `index.html` e `app.html`. Ver `docs/modules.md` (Geração 2).

| Arquivo | Papel |
|---|---|
| `runtime/preferences-runtime.js` | Runtime de preferências do usuário/app |

Único arquivo desta área hoje — não confundir com
`src/shared/config/runtime-config.js` (config de AMBIENTE/deploy, lida
de `<meta>` tags), que é coisa diferente apesar do nome parecido.
